对矩阵的行和列调用函数
  apply()函数允许用户在矩阵的各行或各列上调用指定的函数。
  apply(m,dimcode,f,fargs)
    m是一个矩阵
    dimcode是维度编号，1代表对每一行应用函数，2代表对每一列应用函数。
    f是应用在行列上的函数。
    fargs是f的可选参数集。
    
colMeans()函数求矩阵的列均值
rowMeans()函数求矩阵的行均值
